{% extends 'base.html.twig' %}

{% block title %}Profile Settings{% endblock %}

{% block bread %}
<span>→</span>
<a class="hover:text-indigo-500" href="{{ path('app_settings_profile') }}"> Profile Settings</a>
{% endblock %}

{% block body %}

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    {{ include('settings_profile/_profile_form.html.twig') }}

	<button type="submit" class="block w-full shadow-sm border-transparent bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-400 text-white rounded-md border p-2 mt-4 mb-2">
		Save Setting
	</button>

{{ form_end(form) }}

{% endblock %}


